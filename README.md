# P1 Home Intelligence — Version 14

- Responsive comparison cards with no horizontal scrolling
- School Nearby Mode shows all loaded condos within 2 km
- Unscored nearby condos remain visible
- School marker click switches to nearby-condo mode
- Condo marker click scrolls to the matching result card
- Card selection zooms and highlights the map
- Coverage Audit shows the current database limits

Deploy by replacing the full contents of the GitHub Pages repository with everything inside `public-app/`, then hard refresh with Ctrl + F5.
