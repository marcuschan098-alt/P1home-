# P1 Home Intelligence — Version 13 Release Candidate

Main improvements:
- side-by-side comparison table
- best-value highlighting by metric
- shortlist decision notebook with rating, status, pros, cons and notes
- richer CSV export
- About, methodology and disclaimer page
- V13 cache and version updates

Deploy by replacing the complete contents of the GitHub Pages repository with everything inside `public-app/`. Then hard refresh with `Ctrl + F5`.
