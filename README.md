# P1 Home Intelligence — Version 14.1

This patch fixes the Version 14 startup error:

`clearFocus is not defined`

The missing helper is now included and safely clears map focus layers and card highlighting.

## Deployment

Replace the complete contents of the GitHub Pages repository with everything inside `public-app/`, then hard refresh with `Ctrl + F5`.
